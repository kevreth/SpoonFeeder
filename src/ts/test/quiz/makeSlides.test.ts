import { expect, it } from 'vitest';
// import {MakeSlides} from '../../main/makeSlides';
// import { JSDOM } from 'jsdom';
// const DOC = new JSDOM('<!DOCTYPE html><body></body>').window.document;

it('testShowSlides', () => {
  //set up the global Json object here by addingg slides.
  // MakeSlides.showSlides(DOC);
  expect(0).toEqual(0);
});
it('testShowButton', () => {
  // MakeSlides.showButton(DOC)
  expect(0).toEqual(0);
});
it('testContinueButton', () => {
  // MakeSlides.continueButton(DOC);
  expect(0).toEqual(0);
});
it('testStartOverButton', () => {
  // MakeSlides.startOverButton(DOC);
  expect(0).toEqual(0);
});
