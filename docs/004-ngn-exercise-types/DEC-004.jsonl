{"date": "2026-08-09", "epic": "004-ngn-exercise-types", "context": "PRD-004 originally scoped bowtie as a 3-item linked group built on the linked-group mechanism.", "choice": "Built bowtie as a self-contained aggregate type instead — a single slide with a fixed 5-slot ans/res array (condition + 2 sorted actions + 2 sorted monitors), reusing Evaluate.GAP/Result.CORRELATED unchanged.", "rationale": "Real NCLEX bowtie items are answered as one screen with all three parts visible and submitted together, not as sequential screens. Building it as a linked group would mean 3 separate screens, diverging from real exam presentation. The single-slide encoding needed no new evaluation strategy and no new SlideInterface answer-group fields.", "alternatives": ["3-item linked group rendered as sequential screens", "New dedicated per-answer-group SlideInterface fields"]}
{"date": "2026-08-09", "epic": "004-ngn-exercise-types", "context": "The linked-group mechanism (case-study/trend cluster) needs to construct arbitrary child slide types generically. Doing this as a registered SlideType (the way vocab flattens to Mc) would require the new type's own file to import initSlide/getInstance from slideFactory.ts — the same file that must import the new type's factory to register it — a circular import.", "choice": "Implemented cluster expansion as a special case in jsonProcessor.ts's _initSlide2 (type: cluster is intercepted and expanded via item.set before ever reaching initSlide/getInstance), not as a registered SlideType.", "rationale": "jsonProcessor.ts already imports initSlide and sits outside the slidetype/types layer, so no cycle is introduced. yarn circular enforces zero circular dependencies project-wide as a hard requirement.", "alternatives": ["Register Cluster as a SlideType with getSlideSet() calling initSlide() recursively (circular import)", "Hardcode a small fixed set of importable child factories like Vocab does (loses generality — clusters need arbitrary child types)"]}
{"date": "2026-08-09", "epic": "004-ngn-exercise-types", "context": "Special-casing type: cluster only in jsonProcessor.ts left reviewExtractor.ts's BoundaryMapBuilder/PoolExtractor and scoreProcessor.ts's ScoreProcessor blind to cluster expansion, since all three call initSlide() directly on top-level YAML items. Since cluster isn't a registered SlideType, initSlide() silently fell back to treating it as a single info slide, undercounting lesson/unit/course boundaries by (childCount-1) per cluster and causing the review-boundary prompt to fire mid-cluster — caught by the full Cypress e2e run against review.cy.ts, not by unit tests alone.", "choice": "Added the same type === 'cluster' special case to BoundaryMapBuilder.exercises(), PoolExtractor.exercises(), and ScoreProcessor.exercises(), each iterating item.set directly and adding regression tests to reviewExtractor.test.ts.", "rationale": "Duplicates cluster-awareness across four call sites rather than one, but a shared registered-type approach was blocked by the circular-import constraint from the prior decision.", "alternatives": ["Leave review/score paths uncorrected (rejected — real, reachable bug affecting the review boundary system and ProgressTable.vue's score summary)"]}
{"date": "2026-08-09", "epic": "004-ngn-exercise-types", "context": "Five new Vue exercise components (Bins, ClozeText, ClozeTable, MatrixSingle, MatrixMulti) initialized their state arrays inside onMounted() rather than synchronously, following the same onMounted-init pattern already used by GapExercise/SortExercise.", "choice": "Moved state initialization to run synchronously in <script setup> (immediately after the init() function definition) rather than deferring to onMounted(); onMounted() now only handles the restored-answer case.", "rationale": "The template's first render happens before onMounted fires. GapExercise/SortExercise are safe because their templates guard rendering on the not-yet-initialized array's length (e.g. v-if=\"j < slotLists.length\"); the new components' templates lacked an equivalent guard and indexed directly into the still-empty state array, which crashed outright for MatrixMulti (.has() on undefined) and likely silently mis-rendered for the others. Found via the real Cypress e2e run, not unit tests.", "alternatives": ["Add v-if guards matching Gap/Sort's pattern instead of restructuring initialization (rejected — synchronous init removes the footgun entirely rather than requiring every future new type to remember an equivalent guard)"]}
{"date": "2026-08-09", "epic": "004-ngn-exercise-types", "context": "Extended (partial-credit) multiple response needed a concrete scoring formula; PRD-004 flagged this as TBD against real NCLEX partial-credit rules, which aren't publicly specified in detail.", "choice": "Result.PARTIAL computes max(0, correctSelections - incorrectSelections) / totalCorrectAnswers, floored at 0.", "rationale": "A common, defensible 0..1 partial-credit scheme for select-N items: full credit only for an exact match, proportional credit for partial matches, no negative score for over-selecting.", "alternatives": ["0/1 credit per option against the full option pool (would need the option pool, not just ans/res, available at the Result-strategy call site)", "All-or-nothing scoring (defeats the purpose of building extended SATA at all)"]}
{"date": "2026-08-09", "epic": "004-ngn-exercise-types", "context": "User manual QA after the initial PRD-004 implementation found three presentation bugs via screenshots: (1) matrix-single's table headers didn't align with their data columns, (2) the end-of-course summary showed raw values (\"0\"/\"1\") instead of readable text for bins and matrix-multi answers, (3) a long comma-joined multi-select answer overflowed/got cut off mid-word in the summary's answer pill.", "choice": "(1) Removed a dead global `table { display: block } tbody { display: inline-block }` CSS reset in style1.css — verified via grep that both existing table consumers (.summary-table, .naviTable) already fully override it with their own class-scoped display rules, so it had no real consumer and was silently breaking the new semantic <table> elements. (2) Changed Bins and MatrixMulti to store ans/res as readable labels/names (bin label; sorted comma-joined column names) instead of numeric indices, since Evaluate.GAP's summary row renders raw String(res)/String(ans) with no label lookup. (3) Changed .ans-pill from white-space: nowrap to white-space: normal + overflow-wrap: break-word.", "rationale": "All three were real, user-visible regressions confirmed via Cypress screenshots before and after the fix. (1) was latent dead CSS exposed by being the first real semantic tables in the Vue layer. (2) and (3) were both surfaced by this epic's longer, more realistic option text — the pre-existing ma/gap summary rendering was only ever exercised with short text (single letters, short words) before.", "alternatives": ["(1) Scope the reset to .summary-table specifically instead of deleting it (rejected — confirmed truly unused, deleting is simpler and removes the footgun for future <table> usage)", "(2) Keep indices and add a separate label-lookup step in the summary-rendering path (rejected — more code than just storing the readable value directly, and Evaluate.GAP/makeRow are shared by every GAP-shaped type)"]}
